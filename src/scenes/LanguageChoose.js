import { BaseScene } from "../lib/FontsBase";

export class LanguageScene extends BaseScene {
    constructor(scene) {
        super("Language")
        this.scene = scene

        this.scene.add.image
    }

    chooseLanguaje(){

    }
}